# GUI-Music-Player

A Music Player manufactured wiht the help of `electorn.js` - node js lib. It basically checks the audio files present in the `Music` directory and displays it onto the main container. The color theme is inspired from that of spotify the frontend doesn't uses any external frontend lib.

## Node Js Libraries
- `electron` - for creating the GUI
